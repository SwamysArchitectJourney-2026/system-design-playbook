# Trade-offs: LLM serving

- **Streaming** vs batch response—UX vs simpler infra.
- **Shared** GPU cluster vs **dedicated** tenants—cost vs isolation.
- **Aggressive caching** vs **freshness** and **privacy** (no cache for sensitive prompts).
