# Follow-ups (.NET + Azure)

- **DbContext** lifetime and scoped services under load; connection pool exhaustion.
- **Distributed transactions**—avoid 2PC; use outbox + sagas.
- **APIM** authentication vs API validating JWT—where is trust boundary?
- **Angular** session vs **BFF** cookie vs pure bearer tokens.
- **Blue-green** / slots on App Service for .NET deploys.
