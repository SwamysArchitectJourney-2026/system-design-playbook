# Follow-ups (agentic)

- **Prompt injection** via tool return payloads?
- **Authorization** to tools per user/tenant?
- **Non-determinism** in CI—how to test?
- **Runaway** agent—max steps, budget, kill switch?
- **PII** in memory store—retention and deletion?
