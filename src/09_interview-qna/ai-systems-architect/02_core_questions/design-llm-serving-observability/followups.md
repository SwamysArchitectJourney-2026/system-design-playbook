# Follow-ups: LLM serving

- **KV cache** reuse across similar prefixes?
- **Long context** cost explosion—mitigations (summarization, retrieval)?
- **Deterministic** seed for regression tests?
- **Multi-region**—sticky sessions vs stateless tokens?
